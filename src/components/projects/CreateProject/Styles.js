const useStyles = (theme) => ({
    container: {
        width: '100%',
        height: '100vh',
        paddingTop: 80
    },
    boxCenter: {
        width: '100%',
        height: '80%'
    },
    form: {
        width: '85%'
    }
});

export default useStyles;